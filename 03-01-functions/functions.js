/**
 
function name(params) {
    
}
    


var Name = prompt ("Whats your name")
function sayHello (Name) {
   // alert("Hello" + Name)
}
document.write("Hello" +" " + Name)
myFunction();


function addNumbers("a + b") {
    var c = (a+b);
    return c;
}
document.write(addNumbers(a+b))
*/

function main() {
    var amount = 110;
    var rate = 1.1;
    return (amount * rate)
    }

console.log(amount* rate);